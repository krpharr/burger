<li>
	<div style="display: flex; justify-content:space-between;">
		<div class="col">
			{{#if devoured}}
				
			{{else}}
			<img src="assets/img/burger.sm.png">
			{{/if}}
		</div>
		<div class="col">{{burger_name}}</div>
		<div class="col">
			{{#if devoured}}
			{{else}}
			
				<img class="devour" data-id="{{id}}" src="assets/img/eat.png">
		
			{{/if}}
		</div>
	</div>
</li>
